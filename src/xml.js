dojo.provide("csl.xml");

if (!CSL) {
	load("./src/csl.js");
}

/**
 * Parser modules.
 * <p>A namespace container for wrappers implementing basic
 * commands needed for depth-first navigation of an XML object.</p>
 * @namespace XML parsers
 */
CSL.System.Xml = {};
